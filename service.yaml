apiVersion: v1
kind: Service
metadata:
  name: my-service
spec:
  selector:
    name: my-pod
    ports:
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80

    - protocol: TCP
      port: 8080
      targetPort: 8080
